{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "docker:build": "docker-compose up -d --build",
        "docker:install": "docker-compose exec app composer install",
        "docker:key": "docker-compose exec app php artisan key:generate",
        "docker:migrate": "docker-compose exec app php artisan migrate",
        "docker:full": "npm run docker:build && npm run docker:install && npm run docker:key && npm run docker:migrate"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
